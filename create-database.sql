create database `bruno`
# a linha acima serve para criar um banco de datos com o nome: `bruno`
# o uso da crase se torna necessario parra indicar um `string` do sistema
# `string` do sistema: `database`, `table` e `campo`